# Requirements-to-Evidence Traceability Map

> **Lifecycle:** Current · **Roadmap:** Active

This map distinguishes documented intent from implementation and validation. A proposal, planned fixture, empty project, or future milestone is not validation evidence.

## 1) Scope and status model

The validated requirements cover the dump evaluator through closed W7. The W0–W4 record remains in
`future-work-planning.md`; the completed W5 sequence remains in [`post-w4-path-forward.md`](post-w4-path-forward.md);
and the completed W6 opt-in bounded member-chain sequence remains in
[`post-w5-path-forward.md`](post-w5-path-forward.md), including its common
[C# expression front end and subset admission](../proposals/architecture/csharp-expression-front-end-contract-proposal.md).
The completed W7 sequence is [`post-w6-path-forward.md`](post-w6-path-forward.md): one explicit
`StaticFieldExpressionV1` profile over the complete Roslyn parse, with a context-independent fully qualified binder,
a bounded selected-frame/Portable-PDB import-context slice for simple type names, counted static storage reads, and
reuse of existing W2/W6 suffix evaluation. It is implemented and locally validated for its stated synthetic scope.
The approved but unimplemented W8 sequence is
[`post-w7-path-forward.md`](post-w7-path-forward.md): an additive `StaticFieldExpressionV2` design for bounded
nested/closed-generic namespace/type/member binding, exact scoped aliases/imports/extern aliases, evidence-qualified
current-member/`using static` bare fields, general TypeSpecs, constraints/accessibility/constructed assignability,
stored values, metadata literals, distinct per-construction storage, and predeclared evidence-conditioned
thread/context/RVA/frame-value branches. PM-25
and PM-26 record design intent only; their implementation and validation cells remain empty until evidence lands.
W4.1 fixture-gate evidence, the W4.2 unknown-aware E1/E2 kernel, W4.3's dump-free
non-exact field seam, W4.4's body-independent direct-MethodDef graph preparation, and W4.5a's exact prepared-graph
call execution plus W4.5b's explained-unknown call/return lineage and W4.6's structural model admission, modeled-
return lineage, machine transfer, and compiler/SRM conformance have landed; W4.7's standalone exact-null target-
outcome projection, W4.8's canonical rooted product runner, and W4.9's detached generated-dump execution/replay have
also landed. W4 is closed at exact commit `a819a08fd9ccdf926620c505732475990b242be9`; [hosted run
29463426083](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29463426083) passed all four jobs. Final
documentation-closure commit `aaec73c5b987089addb539d3628de67bd815bd8f` passed them again in [run
29463847230](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29463847230).
W5.1–W5.5b have since landed through `90ade6d92`: the expression-to-result path, deterministic usefulness runner, and
twelve-incident/two-shape meaningful synthetic portfolio are implemented. The designed blocker ranking selects
`AdmitFixedDepthMemberChain` for the next prototype design slice. Generated validation and synthetic design evidence
both contribute zero rows to the representative/external-observation denominator. W5 is closed for its defined
prototype scope under the owner's explicit 2026-07-16 waiver of its hosted-only closure condition; rejected hosted
jobs are not validation evidence. W6.1–W6.7 subsequently closed at exact locally verified source baseline
`440053ad1`, with a separate W6-only owner disposition for the unremediable GitHub billing condition. That disposition
is not hosted pass evidence and does not carry into a successor. W7.1–W7.7 subsequently closed at exact locally
verified implementation source baseline `f99b12ee7`, with a distinct W7-only owner disposition for the same hosted
billing condition and no workflow weakening.
Virtual-debugger and broader analysis requirements remain research so they stay discoverable
without appearing committed.

Status axes:

- **Roadmap relation:** `Active`, `Complete`, `Supporting`, or `Research`. `Active` requirements remain applicable to
  current delivery even when an earlier milestone gate is complete; `Complete` records a closed milestone-scoped
  requirement; `Supporting` records non-gating work outside active delivery.
- **Design:** `Documented`, `Partial`, or `Gap`.
- **Implementation:** `None`, `Prototype partial`, or `Implemented for stated scope`.
- **Validation:** `None`, `Partial`, or `Passing for stated scope`.

“Passing for stated scope” must name an executable test and its proof boundary.

## 2) Requirement ID conventions

- `PM-xx`: active post-mortem evaluator requirements.
- `VS-xx`: virtual-stepping research requirements.
- `X-xx`: cross-cutting requirements shared by multiple potential phases.

## 3) Traceability matrix

| ID | Roadmap relation | Requirement | Design | Implementation evidence | Validation evidence | Next evidence gate |
|---|---|---|---|---|---|---|
| PM-01 | Active | Treat the dump snapshot as immutable evidence. | Documented | `ClrmdDumpSession` holds one read-only stream for content identity and analysis lifetime; its public memory/object operations expose reads only. Interpreter virtual memory is a separate persistent value. W3 imports an exact field observation into a new persistent-memory snapshot and never gives the machine a live dump session. | Passing for stated slice: `DumpMemoryEvidenceIntegrationTests.Full_dump_recovers_rooted_values_with_counted_memory_evidence` generates, hashes, reads, disposes, and deletes a dump without a target-write path. `W3DumpGetterExecutionIntegrationTests.Counted_dump_getters_execute_after_exact_metadata_and_field_correlation` executes direct and adjusted getters over imported immutable evidence and proves unchanged memory plus fresh-session reconstruction. W1 exact-HEAD hosted dump evidence passed in run 29353198889; W3's strengthened implementation checkpoint is `19c292f9f`, and exact documentation-closure commit `de6cea124488d503d13c61a4c8e67203a16d06f9` passed all four required jobs in [run 29375584237](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29375584237). | Complete for W1 and W3; retain the immutable import boundary for every later evidence operation. |
| PM-02 | Active | Read object and method-body facts from real dump memory with provenance and honest partialness. | Documented for the W1 and W3 slices | The ClrMD adapter enumerates strong GCHandles under a scan/match cap, validates the selected handle slot and object-header method table with counted raw reads, and reads `Int32`, bounded/null strings, metadata, and complete tiny/fat method-body shapes from dump memory with exact/partial/unavailable/conflict/invalid outcomes and stable issue codes. The body path obtains the MethodDef RVA from counted dump metadata and reads the physical header, code, local-signature token, padding, and declared extra sections from dump memory. W3 projects the method/signature/field from that same counted metadata, proves the admitted `ldfld` operand names the correlated field, imports only an exact four-byte value, and uses a disk PE only as a late comparison oracle. | Passing for stated slices: W1 real-dump tests verify handle/object pointers, values, string partialness/null, missing field/method, type conflict, invalid address, scan-budget partialness, source IDs, retained reads, a fully dump-sourced tiny `RetOnly` body, and a compiler-emitted fat body with locals and two EH regions. A Normal-vs-Full fixture verifies that an omitted page stays partial/unavailable. W3's two focused tests cover fixed-width snapshot-scoped import identity and counted-dump direct/adjusted getters; the dump is disposed, reopened, and rebound before canonical replay. Non-exact method or field evidence stops before activation/import. W1 exact-HEAD hosted dump evidence passed in run 29353198889; W3's strengthened implementation checkpoint is `19c292f9f`, and exact documentation-closure commit `de6cea124488d503d13c61a4c8e67203a16d06f9` passed all four required jobs in [run 29375584237](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29375584237). | Complete for W1 and the closed W3 E2 getter; extend only with a later admitted evidence shape. |
| PM-03 | Active | Parse and bind a restricted C# expression subset into a read-only query plan. | Documented by the [normative W2 v1 contract](../proposals/architecture/restricted-dump-query-contract-proposal.md); the durable parser boundary is governed by the [common expression-front-end contract](../proposals/architecture/csharp-expression-front-end-contract-proposal.md). | `Interpreter.Product.DumpQuery` implements explicit parse, bind, plan, and evaluate stages for one ordinal exact non-null root, one direct `.` field, and optional bounded null/`Int32`/string coalescing. `DumpQueryRootBinding` preserves exact object, exhaustive absence, partial, unavailable, conflict, and invalid states plus the exact search selector, disposition, issue, counters/caps, retained-match count/limit state, reads, and bounds. Preparation selects the outer field descriptor once, validates snapshot/owner identity, freezes the supported decoder and optional literal into an immutable object-specific plan, and gives that plan an injective canonical v1 identity over the complete descriptor, including nullable child tokens/addresses/sizes. Descriptor evaluation does not repeat outer-member lookup. Exact/null `Nullable<Int32>` decoding uses its validated nested layout; duplicate tokens, overlap, out-of-extent storage, overflow, and forged same-snapshot owners are rejected before value reads. Missing or partial evidence never becomes null. The evaluator never synthesizes or executes IL, and `?.` remains outside v1. W6.2 replaced the handwritten parser with one complete pinned Roslyn parse; project-owned descriptors preserve the W2 behavior. | Passing for the stated W2 slice: the local `ff7cd1965` matrix covers parser, typed-root, descriptor/layout, owner/snapshot, nullable-evidence, plan-identity, and 22 real-dump product cases. Exact closure commit `5bed47100` passed all four required jobs in [GitHub Actions run 29364905178](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29364905178). W6.2 compatibility and complete-parser corpus evidence is recorded by PM-22. | W2 v1 behavior is frozen. PM-22 records the implemented parser migration while PM-20 owns the separately versioned, opt-in second-hop/null-conditional expansion selected by W5; frame roots remain separately gated. |
| PM-04 | Active | Make evaluation deterministic and resource-budgeted. | Documented | Machine budget is operational state separate from semantic state; whole-body admission precedes budget use; query text/identifiers/literals, raw reads, strings, root search, and field selection are capped; handles and canonical content hashes avoid process-random identity. Bound plans retain only the policies whose guarded paths were reached. W3 derives activation shape from frozen metadata, decodes and types the whole body once, resolves each field once, and latches target-null termination so re-stepping cannot spend budget or repeat a load. | Passing for W1 and W2: result context records only reached bounds, with distinct assertions for parser, root, member, foreign-snapshot, null, scalar, nullable, and string paths. The 22-case W2 corpus runs every case through the pipeline repeatedly in one session and after dump reopen/rebind, reproducing all canonical result bytes/hashes and all 13 prepared plan projections/hashes. W3 unit/differential and counted-dump tests reproduce structural handles, admitted shapes, ordered loads, semantic state, memory, budget, events, successful return status, canonical transcript, and SHA-256 in the same machine, a fresh resolver/machine, and after dump reopen/rebind. W4.7 extends the separate null proof with complete same-machine sequence certification, exact accounting, capability-free idempotent re-step, and fresh SRM/domain/machine canonical fragment replay. W1 and W2 hosted evidence remains recorded above; W3's strengthened implementation checkpoint is `19c292f9f`, and exact documentation-closure commit `de6cea124488d503d13c61a4c8e67203a16d06f9` passed all four required jobs in [run 29375584237](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29375584237). | Every later bound or instruction family requires its own path-sensitive and replay assertions. |
| PM-05 | Active | Admit only the evaluator capabilities required by the named scenario. | Documented for the current functional query and closed W3/W4 execution semantics | W2 exposes only bound evidence reads/literal coalescing. W3/W4 interpret only frozen admitted IL. The one W4 pure-model capability receives no memory, dump/session, ambient context, target delegate, or diagnostic text and is invoked only after its exact no-effect descriptor/capability is frozen; no failure falls back to target execution. W4.9 detaches ClrMD evidence before preparation/execution. Reflection is used only by late compiler-fixture oracles. | Passing for the milestone-selected functional scope: exact interpreted/model paths, atomic model failures, body-free modeled planning, resolver/registry poison proofs, read-only memory, detached dump execution, and late-oracle ordering are covered by PM-12 through PM-17. Caveat: no behavior beyond the named fixture shapes is claimed. | Preserve these functional boundaries and require separate evidence before admitting another capability or artifact shape. |
| PM-06 | Active | Report semantic mode, completion, completeness, evidence status, effects, provenance, diagnostics, and evidence context as separate axes. | Documented | `EvaluationResult<T>` implements the independent W2 axes. W4.8's immutable common `CounterfactualExecutionResult` derives the same independent mode/completion/completeness/evidence/effect axes plus value/prefix, rooted or standalone context, assumptions, models, bounds, call/model/event transcripts, provenance, and diagnostics from issuer-certified evidence. W4.7's standalone fragment deliberately has no rooted identities; W4.8 rooted results retain canonical request/plan identities and W4.9 dump results additionally retain dump-snapshot/root evidence. | Passing for W1/W2 and W4: W4.8's result matrix asserts every axis across exact, degraded, budget, cancellation, blocked, invalid, modeled, and standalone outcomes; W4.9 reproduces rooted exact/degraded dump results after close/reopen. Tests reject historical/causal replay language and preserve the standalone fragment's deliberate absence of rooted reachability. | PM-06 is complete for the implemented W4 profile. Preserve the separate axes and do not conflate standalone target evidence with rooted reachability. |
| PM-08 | Active | Degrade cleanly when optimized dumps omit roots, locals, arguments, or `this`. | Documented | W2 typed root binding distinguishes exhaustive absence, partial, unavailable, conflict, and invalid evidence and never reinterprets missing evidence as null. Missing/partial fields and nullable payloads also retain their typed evidence rather than fabricate null or a scalar. Dump-frame seeding still does not exist: W3 `ActivateRoot` receives explicit receiver/argument values, and its counted-dump proof imports only one exact rooted field cell rather than recovering a frame. A versioned generated optimized Release modeled-incident target keeps `this`, argument, local, static, and strong-root axes in a canonical report. | Passing for W1/W2 missing-evidence semantics and W7 static attribution: the W2 corpus covers exhaustive root absence, bounded partial root selection, missing fields, and partial strings; adapter tests cover partial nullable payloads. W3 proves that an absent imported field blocks after exactly one load-capability call, without completing the `ldfld` semantic transfer or fabricating a default. The current generated schema-v2 optimized-context report records raw member bytes at 5/5, attributable context at 2/5 after exact static-slot attribution, and product-query availability at 1/5; the static and strong-root axes are attributable, while only the strong-root query is available in that report. Earlier W1 evidence remains historical. This is not representative external evidence. | Representative observation remains a non-gating product-readiness follow-up; W8.1 separately probes exact frame value/generic-context attribution and must preserve unavailable/partial semantics. |
| PM-09 | Active | Admit the branchless rooted `DumpProbe.GetMarkerSummary() => CombineMarkers(Marker, AlternateMarker)` workflow as W4's product-value gate because W2 cannot read two fields or execute either method. | Documented by the [normative W4 contract](../proposals/architecture/counterfactual-method-evaluation-contract-proposal.md) | Implemented for W4.1 at pushed checkpoint `82363585b`: the target readiness oracle executes the two methods; PE/SRM tests freeze exact caller/helper IL, relational fields/call target, signatures, body facts, and `NoInlining`; CoreCLR returns `0x26AF37BD`; W3 executes the helper but truthfully rejects the caller's second `ldfld` at offset 7 before the direct `call` at offset 12. This is fixture/gap evidence, not W4 call execution or a product result. Realized surface: 478 added or materially revised LOC. | Locally passing: locked restore; 15-project zero-warning Release build; focused W4.1 4/4; complete fast 71/71; ordinary dump regression 5/5; zero skips; every test command headless with the milestone test selection. Hosted slice evidence is not required until W4.9 closure and is not claimed here. | Preserve the exact fixture and W3 rejection boundary while later slices add independently tested capability; do not reinterpret either as call execution. |
| PM-10 | Active | Propagate provenance-bearing unknowns through the closed W3 E1/E2 transfer set without fabricating concrete values or weakening structural typing. | Documented by the [normative W4 contract](../proposals/architecture/counterfactual-method-evaluation-contract-proposal.md) | Implemented for the stated W4.2 scope at pushed checkpoint `e89e43498`: `IValuePrecisionDomain<TValue>` and an explicit machine policy admit only validated, locally owned explained `Int32` unknowns; `ProvenanceConcreteDomain` preserves lifted-flat semantic equality independently of explanation; and versioned content-addressed `InputOrigin`/ordered `BinaryTransform` nodes replay through the existing argument/local/arithmetic/return handlers. Exact E2 `ldfld` remains exact, while partial/unavailable field observations were deliberately deferred to W4.3 and are now recorded separately in PM-11. The implementation is 3,429 LOC (1,521 production plus 1,908 tests) against the final refined 3,350–3,500 LOC estimate. It creates no W4 product facade or dump-grounded counterfactual result. | Passing for stated scope at `e89e43498`: focused W4.2 53/53 (23 domain, 14 lineage, 16 machine); complete unit 156/156; fast 71/71; ordinary dump 5/5; strict 15-project Release build with 0 warnings/errors; Markdown/headless guards; and zero skips. Every managed command was headless and every test command used the milestone test selection. | PM-10 is complete for W4.2. Preserve its lifted-flat and exact-field invariants through later W4 slices; PM-11 records the separately landed W4.3 field continuation. |
| PM-11 | Active | Continue from non-exact dump field evidence only where the W4 contract admits a typed unknown, retaining evidence status, reason, source, and read geometry. | Documented by the [normative W4 contract](../proposals/architecture/counterfactual-method-evaluation-contract-proposal.md) | Implemented for the stated dump-free W4.3 scope at checkpoint `7479b1ad4`: immutable, content-equal `FieldLoadEvidence` canonically retains the frozen ordinary instance `Int32` field, partial/unavailable status, stable reason, complete source/imported-object identities, address, requested/observed lengths, and copied observed bytes; `MemoryLoadResult.FromFieldEvidence` transports it without changing legacy code-only outcomes. The generic `ldfld` handler continues only when structured evidence matches the frozen field, `UnknownExecutionPolicy.ExplainedInt32` is enabled, and the domain implements `IFieldLoadApproximationDomain<TValue>`. Exact remains exact; code-only partial/unavailable and absent policy/capability block atomically, conflict remains blocked, and invalid or mismatched evidence remains invalid. Success preserves memory and emits `InstructionExecuted` then `ValuePrecisionLost`. `ProvenanceConcreteDomain` binds a canonical imported-field `InputOrigin` to `FieldLoadTransform`, excludes the local reference number, preserves W4.2 identities, and replays in fresh domains. Realized surface is 3,096 LOC: 1,100 production LOC plus 1,996 test LOC. No ClrMD adapter, product facade, or dump-grounded W4 result is implemented. | Passing for stated scope at `7479b1ad4`: strict 15-project Release build with 0 warnings/errors; focused W4.3 55/55; complete unit 211/211; fast 71/71; ordinary dump regression 5/5; optimized dump regression 1/1; Markdown/headless guards; and zero skips. Every test command was headless and used the milestone test selection. | PM-11 is complete for its stated dump-free W4.3 scope. Preserve this kernel evidence while later slices add execution and product composition; do not reinterpret it as ClrMD import or a dump-grounded counterfactual result. |
| PM-12 | Active | Resolve the admitted direct helper call through structural metadata and freeze the complete caller/callee dependency closure before instruction zero. | Documented by the [normative W4 contract](../proposals/architecture/counterfactual-method-evaluation-contract-proposal.md) | Implemented for W4.4 in two pushed checkpoints: `2e596c117` adds content-equal body-independent `MethodCallSignatureShape`, exact managed-IL `ResolvedMethodCallTarget`, and contextual same-module direct-MethodDef resolution without acquiring a body; `742ef2c4f` adds `MethodGraphPlanner.Prepare` and immutable `FrozenMethodGraphPlan`/node/call-site projections. Preparation decodes and types the root and every reachable callee, correlates call descriptors with loaded definitions, deduplicates shared callees, rejects self/mutual cycles, and freezes canonical nodes, fields, edges, required logical depth, and internal traversal units before returning success. Conflict remains `Conflict`, and every failure exposes no partial plan. The fixed internal resource ceilings are 64 distinct methods and 1,024 method/field/edge units; they are implementation guards, not the configurable product traversal budget. Realized W4.4 surface is 3,651 added LOC: W4.4a is 1,043 LOC and W4.4b is 2,608 LOC, totaling 2,076 production plus 1,575 test LOC. | Passing for stated scope after `742ef2c4f`: locked restore; strict 15-project Release build with 0 warnings/0 errors; focused planner 35/35; focused W4 fixture 6/6; complete unit 250/250; fast 73/73; ordinary dump regression 5/5; optimized dump regression 1/1; both repository guards; and zero skips. Every behavioral command was headless and used the milestone test selection. The fixed fixture freezes exactly two method nodes, two fields, one edge at caller IL offset 12, required depth 2, and five internal units. The legacy `IlMachine` still rejects before the call, proving preparation without claiming call execution. | PM-12 is complete for W4.4. W4.5a now consumes the frozen graph without re-resolving metadata; preserve preparation's complete/no-partial-plan boundary through later slices. |
| PM-13 | Active | Execute the admitted call through deterministic multi-frame push/return boundaries and a prepared maximum-logical-depth bound, keeping return-site identity, exact or explained-unknown values, state, memory, configured/required depth, observed high-water marks, and call/return lineage replayable. | Documented by the [normative W4 contract](../proposals/architecture/counterfactual-method-evaluation-contract-proposal.md) | Implemented through W4.5 at `356c07037`/`c72f6ee9e`: exact and explained-unknown direct calls preserve structural return sites, one-instruction transfers, memory, ordered frame events, configured/required/high-water depth facts, and append-only canonical parameter/return lineage. W4.5 realizes 6,138 LOC. W4.6 preserves those interpreted identities and adds a separate frame-free modeled boundary with logical-depth accounting; W4.8 projects both through the product runner and W4.9 proves both from detached dump evidence. | Passing for stated scope at `c72f6ee9e`: prepared graph 40/40; combined lineage/audit 76/76 including 29 legacy identities; compiler lineage 2/2; W4 integration 9/9; unit 297/297; fast 76/76; dumps 5/5 and 1/1; strict build/restore; zero skips; and the milestone test selection. W4.6c/d additionally prove modeled logical/active depth 2/1; W4.8/W4.9 preserve those facts in canonical product results. | PM-13 is complete. Preserve the same transfer and accounting invariants in product and dump-backed execution. |
| PM-14 | Active | Represent the one admitted pure model with typed return/effect/fallback outcomes; unmodeled or effectful ambient behavior fails closed with stable explanation. | Documented by the [normative W4 contract](../proposals/architecture/counterfactual-method-evaluation-contract-proposal.md) | Implemented through W4.6. W4.6a `77c92789b` freezes exact/no-effect structural selection and a body-free leaf with no fallback or partial plan. W4.6b `fd723a912` appends atomic schema-v1 kind-6 `ModeledReturnTransform` while preserving kinds 1–5. W4.6c `877c9fb55` invokes only the frozen capability: exact or grounded unknown returns atomically transfer in the caller, preserve memory, consume one instruction/event, and create no helper frame; every entered invocation records an operational attempt, while failures preserve semantic state, memory, budget, and semantic events. Logical-depth high water includes a failed model entry while active-frame high water does not. W4.6d `da5346813` proves exact and degraded compiler/SRM conformance without helper-body acquisition or execution-time resolver/registry/descriptor/selection rereads. W4.8 projects that behavior; W4.9 executes it from detached dump evidence while the helper body is unavailable to the modeled resolver. W4.6 realizes 7,652 LOC. | Passing for the closed model scope: interpreted/model/CoreCLR agree for exact input and interpreted/model agree for degraded input. The mixed and dual-unknown graph hashes remain frozen; W4.8 covers exact/unknown/failing model product rows and W4.9 covers exact/partial/unavailable body-free modeled dump rows with reopen replay. All required behavioral filters use the milestone test selection. | PM-14 is complete. Preserve the one exact no-effect model, no target fallback, no helper-body acquisition, and non-throwing-model rule. |
| PM-15 | Active | Define a standalone retained exact typed-null target outcome for later counterfactual-result-projector integration while keeping fabricated rooted requests/plans, helper/model throws, handler transfer, filters, unwind, `finally`, and `fault` excluded. | Documented by the [normative W4 contract](../proposals/architecture/counterfactual-method-evaluation-contract-proposal.md) | Implemented at pushed W4.7 checkpoints `2e70fe76d`/`dad6a6dd4`. `CounterfactualTargetOutcomeProjector` requires the complete same-machine sequence of exact issued transitions from legacy IL-zero activation to the first exact null-reference latch, optionally plus one issuer-certified idempotent re-step. It validates location, memory identity, accounting, and complete events, then emits the standalone schema-v1 fragment with no snapshot/root/request/plan/traversal facts. W4.8 integrates that fragment into the common result projector without adding rooted reachability. W4.7 realizes 2,801 LOC. | Passing for stated scope: focused W4.7a 15/15, W4.7b 2/2, compiler differential 23/23, and W4.8 common-result integration. Direct/adjusted compiler getters reproduce fresh; poisonable/counting capabilities prove re-step performs no capability call or repeated load. Literal fragment SHA-256 is `a9b98e46583dcf90ac108571c126d8d86cec0465c595e2689fae767e33ff108e`. | PM-15 is complete. Preserve the standalone fragment's absent rooted identities and the excluded handler/helper/model-throw behavior. |
| PM-16 | Active | Expose one canonical product facade/runner whose result is `CounterfactualExecution` and keeps assumptions, models, effects, evidence, completion, completeness, reached bounds, target outcome, provenance, and diagnostics separate. | Documented by the [normative W4 contract](../proposals/architecture/counterfactual-method-evaluation-contract-proposal.md) | Implemented through W4.8 final checkpoint `44b050ec8`: configurable ordered traversal accounting, immutable schema-v1 requests/observations/issued plans/results, private typed runtime bindings and recording memory, deterministic authoritative preparation, transition-validating execution, and one common rooted/standalone projector. Allocation remains unadmitted and has no dormant budget counter. W4.8 realizes 11,924 attributable implementation LOC across six delivery checkpoints. | Passing for the complete W4.8 scope: focused runner 10/10, counterfactual family 77/77, complete milestone-selected unit 502/502, strict Release compilation with zero warnings/errors, same/fresh-object canonical replay, and exact synthetic rooted result SHA-256 `8eb243d415e739a9101f0dd7b8e1cdb4de38208069caa495b8c1bd02a37b7d09`. | PM-16 is complete. Preserve issuer authority, defensive immutability, independent product axes, and no invented rooted reachability for the standalone target fragment. |
| PM-17 | Complete | Close W4 with exact/degraded generated-dump corpus coverage, CoreCLR differential evidence, same/fresh-session canonical replay, and exact-commit hosted CI within the milestone test selection. | Documented by the [normative W4 contract](../proposals/architecture/counterfactual-method-evaluation-contract-proposal.md) | Implemented through W4.9: `24bd8fe6f` adds the canonical ClrMD method/field evidence graph, `2d41f528d` adds the product-owned detached rooted binding/memory, and `a8b5f32f0` executes six exact/partial/unavailable interpreted/modeled dump rows after ClrMD disposal and reproduces canonical memory/request/plan/result artifacts after close/reopen/rebind. W4.9 realizes 2,698 LOC; cumulative W4 realizes 41,892 LOC excluding documentation and generated lockfile changes. | Local closure passes locked restore, strict Release 0/0, unit 502/502, Fast 88/88, dumps 6/6 and 1/1, aggregate W4 14/14, and guards 62/44 and 1. [Hosted run 29463426083](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29463426083) passed documentation, build/Fast/unit, real-dump, and optimized-dump jobs at implementation-closure commit `a819a08fd9ccdf926620c505732475990b242be9`; [run 29463847230](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29463847230) passed the same matrix at final documentation-closure commit `aaec73c5b987089addb539d3628de67bd815bd8f`. | PM-17 and the W4 umbrella are complete for the defined milestone-selected scope. Broader scenarios remain gated research, not closure debt. |
| PM-18 | Complete | Accept one product expression and return one honest W2 derived-query or W4 counterfactual result without caller-assembled metadata handles, method graphs, field vectors, domains, or machine state. | Documented by the [Post-W4 Path Forward](post-w4-path-forward.md) | W5.1–W5.4 checkpoints `7c3d52572`/`d88b13c2c`/`fc8a43a7a`/`59d9bb590` implement exact admission, typed dump acquisition, detached binding, strict mode-preserving outcome union, and the external headless consumer. W2 retains `EvaluationResult<DumpQueryValue>`; W4 retains execution/preparation identity; classification and acquisition failures gain no fabricated semantic mode. | Passing for the admitted generated shape: exact field and exact interpreted/modeled method rows, degraded completed unknowns, unsupported expression, typed acquisition failure, budget, cancellation, repeated same-session execution, fresh processes, and dump reopen all retain their canonical underlying artifacts. The focused W5 facade/reference-consumer lanes pass headlessly with zero skips at their pushed checkpoints. | PM-18 is complete for the exact W2 admitted shape and exact `root.GetMarkerSummary()` W4 form under the historical parser. Other bindings, roots, methods, and application shapes remain outside the claimed boundary. |
| PM-19 | Complete | Measure meaningful predeclared incident-question usefulness before admitting another prototype syntax, context, opcode, EH, model, or debugger increment; keep external field-readiness evidence separate. | Documented by the [Post-W4 Path Forward](post-w4-path-forward.md) | W5.5a checkpoints `0f5230e13`/`b788f4f66` implement schema-v2 deterministic raw admission/outcome/usefulness/acquisition composition, per-question evidence and decision impact, strict generated/synthetic/representative corpus separation, and predeclared synthetic root/scenario/outcome/value contracts. W5.5b checkpoint `90ade6d92` adds twelve independent dumps across request-pipeline and batch-pipeline object graphs. | Fresh hidden consumers validate one question per snapshot; all 12 hashes are distinct; two fresh portfolio processes produce byte-identical reports. Counts are 8/12 admitted, 4/12 exact, 2/3 useful partial-or-unknown, and 6/12 decision-changing. The blocker ranking is member navigation 4, context acquisition 3, and execution body 1, selecting `AdmitFixedDepthMemberChain`. Representative/external observations remain 0/0/0, no percentage or readiness claim is emitted, and the generated-to-representative negative test remains passing. Exact source baseline `26ba75fb1` passes the complete local closure matrix. Hosted run 29512657137 passed documentation and Build/Fast but did not execute either dump job; run 29513051897 executed no jobs. | PM-19 and W5 are closed under the explicit 2026-07-16 owner waiver of W5's final hosted-only condition. The rejected jobs remain non-evidence, the workflow remains unchanged, and the exception does not authorize the selected fixed-depth member-chain slice without its own contract. External observations may later confirm, reverse, or stop that prototype direction. |
| PM-20 | Complete | Under an explicit `FixedDepthMemberChainV1` profile, bind one exact root field to an exact null/non-null or typed non-exact reference observation, structurally bind one terminal direct field or certified field-backed data property, and return an honest derived-query result without repeated lookup or getter execution. | Documented by the [Post-W5 Path Forward](post-w5-path-forward.md) and [common expression-front-end contract](../proposals/architecture/csharp-expression-front-end-contract-proposal.md). | W6.1 checkpoint `0dc055e66` freezes the four graph families and twenty-four target invocations. W6.2 checkpoint `68aaf418f` pins one complete Roslyn expression parse, deletes the handwritten reader, and emits only versioned project-owned admitted descriptors. W6.3 checkpoints `779691cb3`/`4eacba80c`/`6c36bd397` add counted dump-metadata declaration certificates, MethodDef-token physical getter acquisition, exact property/backing correlation, three decoder-relative layouts, and bounded catalogs without reading a value. W6.4 checkpoints `ee788e414`/`40ece4446` add truthful four/eight-byte reference observations, separately comparable intrinsic non-root identity and alias-path provenance, exact target/header/extent validation, descriptor-only terminal readers, a canonical complete plan, typed no-partial-plan failures, and a preparation/evaluation capability seam. W6.5 checkpoints `2055fd750` through `62c4bb157` add frozen-plan evaluation, explicit facade routing, independent uncertainty axes, and schema-v2 headless conformance without altering the default W2/W5 route. | The W6.5 generated lane evaluates eleven independent full snapshots through same-session repetition and two fresh reopened consumer processes per row. It covers exact fields/properties, strings/integers/nullable integers, exact-null conditional/fallback behavior, direct-null blocking, partial/unavailable/conflict/invalid evidence, incompatible fallback, partial-string completion, typed non-exact roots, admitted binding misses, and valid unsupported C# while W5 schema-v1 replay remains passing. The exact selected answers are request-failed, running, batch-failed, and running. | PM-20 is complete. Evaluation consumes only the immutable plan, performs one reference read, never rebinds declarations or invokes getters, and preserves independent value/evidence/completeness axes. Other parse trees and runtime behaviors remain outside the admitted profiles. |
| PM-21 | Complete | Close W6 through an external headless consumer, generated conformance corpus, independent meaningful synthetic dumps, deterministic replay, a threshold-qualified next decision or explicit deferral, and exact-commit repository gates. | Documented by the [Post-W5 Path Forward](post-w5-path-forward.md) | W6.5 implements the generated schema-v2 conformance lane. W6.6 checkpoints `93c1f684b`/`440053ad1` add an append-only schema-v3 runner and twenty-four predeclared incidents across request, batch, coordinator, and workflow graph shapes. Every incident owns an independent full snapshot and fresh hidden consumer. Raw admission, outcome, usefulness, decision, and boundary counts remain separate; representative counts stay zero; promotion is rejected; a unique-leader floor selects only `AdmitOneConcreteContextAcquisitionScenario`; and an exact substantive tie defers. | Exact local source baseline `440053ad1` passes locked restore; strict Release at 0 warnings/errors; unit 502/502; Fast 121/121; complete integration 144/144; focused W6 2/2; optimized context 1/1; Markdown 65 files/89 local destinations; one headless workflow; the authored-scope vocabulary guard; and zero skips. Two fresh portfolio processes are byte-identical. The owner's separate 2026-07-16 W6-only disposition closes the hosted-only block caused by GitHub billing without claiming hosted execution or weakening the workflow. | PM-21 and W6 are complete for the defined synthetic prototype scope. The selected context-acquisition category was not implemented by W6; closed W7 subsequently turned it into a static-field expression plus selected-frame/PDB/import binding slice. Representative production measurement remains separate, and the W6-only hosted disposition does not carry forward. |
| PM-22 | Complete | Parse every bounded product expression once with a pinned complete Roslyn C# expression front end, then admit and evaluate only explicitly versioned project-owned tree shapes. | Documented by the [C# Expression Front-End and Subset-Admission Contract](../proposals/architecture/csharp-expression-front-end-contract-proposal.md), W6.2 in the [Post-W5 Path Forward](post-w5-path-forward.md), and W7 in the [Post-W6 Path Forward](post-w6-path-forward.md). | W6.2 checkpoint `68aaf418f` centrally pins `Microsoft.CodeAnalysis.CSharp/5.3.0` only on `Interpreter.Product.DumpQuery`, performs one C# 14 regular-source/full-text parse, applies integrity and complexity bounds, and runs ordered disjoint W2/W5/opt-in W6 recognizers. It deletes the production handwritten reader/literal decoder and W5 diagnostic-dependent string route, freezes a new tagged chain identity, passes the direct-member descriptor to preparation, and leaks no Roslyn type beyond the front-end boundary. W7 checkpoint `f99b12ee7` routes `StaticFieldExpressionV1` through the same internal complete-expression entry point, leaving one direct production `SyntaxFactory.ParseExpression` call; every later binder/evaluator consumes only its detached project-owned descriptor. | The complex three-bucket corpus, exact package/lock/dependency guards, one-parse-site source guard, public-surface reflection guard, legacy parser diagnostics, W2 dump replay, W5 fresh reports, generated W6 conformance, W6 portfolio, and all W7 parser/binder/portfolio lanes pass. The W7 local closure totals include unit 507/507, complete integration 242/242, Fast 184/184, focused W7 98/98, and zero skips. | PM-22 is complete for W2/W5/W6/W7 parsing and subset admission. Complete C# binding or evaluation remains explicitly out of scope: Roslyn owns syntax, while versioned project recognizers and the binder/evaluator own the supported semantic subset. |
| PM-23 | Complete | Bind and read an ordinary static field directly from an admitted Roslyn expression: guarantee non-ambiguous fully qualified lookup without frame/PDB context, admit contextual names when exact selected-frame/Portable-PDB current-namespace/import/alias evidence yields one symbol, and optionally reuse the existing W2/W6 suffix evaluator for a validated reference value. | Documented by the completed [Post-W6 Path Forward](post-w6-path-forward.md). | W7 implements the sole-Roslyn-parse `StaticFieldExpressionV1` descriptor; unique counted-metadata fully qualified and contextual binders; selected-frame/mapped-PE/debug-identity/Portable-PDB/LocalScope/ImportScope acquisition; immutable context/symbol/runtime/storage/value/provenance identities; raw ordinary static scalar/string/nullable/reference decoding; target assignability validation; lazy context acquisition; and unchanged W2/W6 suffix composition. Fully qualified requests do not invoke context resolution, and no failed stage exposes a candidate or falls back. | Passing for the stated W7 scope: the exact implementation source baseline `f99b12ee7` passes focused W7 98/98, `StaticFieldExpressionV1` 1/1, optimized context 1/1, complete integration 242/242, and unit 507/507 with zero skips. Parser architecture guards prove one production complete-parse site; real generated dumps, identity-matched and conflicting PDBs, and a duplicate-type assembly cover exact and typed non-exact boundaries. | Complete for W7. Preserve the context-independent fully qualified floor and evidence boundaries. PM-25 separately approves an additive, versioned V2 expansion without altering this evidence. |
| PM-24 | Complete | Close W7 with headless generated static-expression/context conformance, sixteen independent meaningful synthetic dumps over four unrelated shapes, deterministic replay, and a threshold-qualified next decision or explicit deferral. | Documented by the completed [Post-W6 Path Forward](post-w6-path-forward.md). | `w7-static-field-incidents-v1.json`, `Interpreter.W7TestTarget`, the conflict-PDB and duplicate-type fixtures, and the reference-consumer W7 portfolio mode materialize sixteen independent full dumps split evenly across request, batch, coordinator, and workflow shapes. Each row owns raw/control expressions, context/artifact inputs, exact terminal expectations, usefulness/decision facts, suffix profile, and first boundary. Two fresh hidden consumers emit byte-identical machine and human reports. | The portfolio lane passes 1/1 and proves exact fully qualified/current-namespace/import/type-alias results, W2/W6 suffix reuse, exact null/nullable-no-value, missing frame, partial/mismatched PDB, import ambiguity, duplicate module type, field absence, unavailable/partial slot, target conflict, invalid signature, and unsupported syntax. Representative counts remain zero; promotion is rejected; a substantive tie defers; and the manifest-assigned `BindingContextPrecision` label selects the permitted action name `AddOneEvidenceBackedFramePdbImportAliasGenericRule`. The complete local matrix and repository guards pass. The W7-only owner disposition records rejected hosted jobs without claiming execution or weakening the workflow. | Complete for W7's synthetic prototype scope. W7 permits choosing one rule along that trajectory but does not prove which rule; the separately approved W8 plan makes its inclusive V2 decision explicitly. |
| PM-25 | Active | Under explicit `StaticFieldExpressionV2`, bind stored and literal static fields through bounded nested and closed constructed generic namespace/type/member lookup; preserve context-free fully qualified lookup; admit exact current-type/scoped type/namespace/extern aliases, general closed TypeSpecs, bare current members and evidence-qualified `using static`, generic constraints/accessibility/field substitution, constructed assignability, metadata literals, and distinct per-construction storage; resolve predeclared thread/context/RVA and optional `FrameValueExpressionV1` branches from W8.1 evidence; and never guess an open construction, lexical owner, location, or slot. | Documented by the active [Post-W7 Path Forward](post-w7-path-forward.md) and governed by the [common expression-front-end contract](../proposals/architecture/csharp-expression-front-end-contract-proposal.md). | None. W8 is design-approved but no V2 source or public surface has landed. | None. Planned compiler/PDB/runtime/storage probes, contract/parser/binder/runtime/product conformance, and real-dump rows are not current evidence. | W8.1 must first prove real TypeSpec/extern/using-static/literal facts, class/value-type/interface constructions with distinct slots/values, constraints/accessibility/assignability, nested/base ownership, exact runtime argument topology, storage/frame branch dispositions, and reopen replay. |
| PM-26 | Active | Close W8 through complete headless generated conformance, thirty-two core independent meaningful synthetic dumps plus one for every W8.1-admitted evidence branch over four materially distinct shapes, deterministic replay, corrected usefulness/decision/attributable-stage metrics, one threshold-qualified successor or explicit deferral, exact-commit repository gates, and honest hosted evidence. | Documented by the active [Post-W7 Path Forward](post-w7-path-forward.md). | None. The W8 targets, append-only report schemas, portfolio runner, and consumer modes do not yet exist. | None. The planned corpus is synthetic design intent, contributes zero representative rows, and cannot be counted before materialization and execution. | Complete W8.1–W8.9 in order, preserve every W1–W7 golden, then close W8.10 only at the exact pushed commit after the full zero-skip headless matrix and hosted jobs, absent a new W8-only owner disposition. |
| VS-01 | Research | Define predictable Step Into/Over/Out across interpreted and modeled calls. | Documented research | No debugger controller/product implementation. | None. | Entry gate: W4 method execution, deterministic instruction/frame/model events, and stop-on-throw; the later stepping increment owns pause semantics, and Step Out through handlers additionally requires handler-transfer EH. |
| VS-02 | Research | Explore unknown branches through explicit counterfactual choices. | Documented research | None. | None. | Entry gate: validated unknown-aware method execution and bounded branch policy. |
| VS-03 | Research | Map steps through PDB, decompiled source, then IL fallback. | Documented research | No symbol/decompiler/debug-map implementation. | None. | Entry gate: virtual stepping is approved and a real stepping fixture needs source mapping. |
| VS-04 | Research | Represent async and dynamic outcomes explicitly. | Documented research | None. | None. | Entry gate: scenario-derived interpreter, call, generic, and EH prerequisites pass. |
| X-01 | Active | Preserve stable runtime/metadata/artifact identity, evidence provenance, and normalized miss reasons. | Documented for current slices | Dump metadata-root identity is MVID plus exact metadata length/SHA-256. A disk-backed module handle carries complete-artifact identity; W3 snapshot-scoped module handles instead combine counted metadata identity with stable runtime-module source identity. Structural `TypeSig`, `MethodHandle`, and `FieldHandle` values include their content/snapshot-derived module and non-nil metadata tokens. Resolved method shape/body and resolved field owner/type/storage facts are frozen atomically. Dump snapshot/runtime-module identities remain distinct; imported owners retain a bounded domain-separated evidence identity. | Passing for W2 and the stated W3 slice: prior copy-path, patched-artifact, foreign-snapshot, descriptor, and replay cases remain. W3 adds cross-module non-aliasing, default/nil rejection, SRM structural signature/FieldDef projection, snapshot/loader distinction, exact `ldfld` operand correlation, fixed-width imported-owner identity, and same/fresh/dump-reopen identity replay. The disk body remains only a late equality oracle for dump execution. W3's strengthened implementation checkpoint is `19c292f9f`, and exact documentation-closure commit `de6cea124488d503d13c61a4c8e67203a16d06f9` passed all four required jobs in [run 29375584237](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29375584237). | Add a representative artifact-resolution candidate set only when a scenario requires it; do not weaken W3 structural or snapshot-scoped identities. |
| X-02 | Active | Keep machine execution status, host pause reasons, diagnostics, and adapter miss reasons distinct but explicitly mapped. | Documented | Implemented `MachineRunStatus`/`ExecutionFailure`, structured `TargetExceptionInfo`, memory-load result kinds, and `ClrmdEvidenceStatus`/`ClrmdValueIssue` contracts remain distinct. Resolver/domain/memory capability failures are normalized without copying exception text. The future session-pause vocabulary and allowed mapping live only in architecture until a controller needs code. | Partial repository-wide, passing for W3 machine semantics: tests cover ready/completed/budget/blocked/invalid/target-exception outcomes, truthful failure origins, payload-omitting resolver/plugin exceptions, exact versus non-exact memory loads, and an idempotent terminal null-receiver state. Adapter tests cover evidence categories; a session controller does not yet exist. | Keep the W4 product runner on machine/product-result vocabularies. Introduce a session-pause contract only if a later virtual-session controller is admitted; do not reserve it in the W4 machine API. |
| X-03 | Research | Suppress framework machinery through semantic models while preserving effects and explanations. | Documented research | None. | None. | Entry gate: two implemented models demonstrate a repeated extension need. |
| X-04 | Research | Keep environment/time/random behavior session-stable and explicit. | Documented research | None. | None. | Entry gate: an approved counterfactual scenario requires an environment intrinsic. |
| X-05 | Research | Make projection-backed collection reads bounded, version-aware, and fail-closed. | Documented research | None. | None. | Entry gate: a W2/W4 user scenario cannot be served by public-layout reads. |
| X-06 | Research | Reuse one semantic engine across concrete, unknown-aware, and abstract modes. | Partial hypothesis | W3's concrete domain and W4.2's provenance-aware lifted-flat domain execute through the same metadata-derived activation and argument/local/constant/arithmetic/exact-field/return handlers. W4.3 extends the same generic `ldfld` handler: both domains retain exact behavior, while the explicit policy and optional field-approximation capability allow only the provenance-aware domain to continue from matching structured partial/unavailable evidence. Explanation remains outside semantic equality, and no abstract-analysis mode exists. | Passing for the shared concrete/unknown-aware handler scope through `7479b1ad4`: focused W4.3 55/55 and complete unit 211/211 cover exact and approximate `ldfld` through the same handler, policy/code-only/no-capability stops, conflict/invalid behavior, atomic malformed-capability rejection, ordered `InstructionExecuted`/`ValuePrecisionLost` events, arithmetic continuation, canonical lineage, and fresh replay. Fast 71/71 plus ordinary 5/5 and optimized 1/1 dump regressions preserve the earlier exact paths; all test commands were headless with the milestone test selection. | Treat concrete/unknown-aware shared-handler reuse as demonstrated. Any broader “one engine, all modes” claim still requires a scenario-derived abstract domain and measured shared-handler evidence. |
| X-07 | Active | Admit the closed W3 concrete IL profiles only from structural metadata and exact imported evidence. | Documented by the [normative W3 contract](../proposals/architecture/concrete-il-execution-contract-proposal.md) | Structural `TypeSig`, `MethodHandle`, and `FieldHandle` identities feed atomic `ResolvedMethodDefinition`/`ResolvedField` projections from `SrmMetadataProjection`; `ActivateRoot` derives frame shape; `MethodPlanBuilder` freezes typed whole-body plans; injected `IMemoryModel` and `ConcreteMemory` distinguish allocated defaults from imported absence; and `ClrmdDumpExecutionResolver` composes counted metadata/body/owner/field evidence without giving the machine a live dump session. | Passing for the implemented scope: identity/SRM/admission/memory unit tests, `CompilerEmittedDifferentialTests`, and `W3DumpGetterExecutionIntegrationTests` cover strict E1/E2 profiles, negative suffixes, arithmetic/overflow/getter/null CoreCLR agreement, exact-one-load behavior, same/fresh-session replay, and dump reopen/rebind. Strengthened implementation checkpoint `19c292f9f` and exact documentation-closure commit `de6cea124488d503d13c61a4c8e67203a16d06f9` are both hosted-verified; [closure run 29375584237](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29375584237) passed all four required jobs. | Any added opcode, signature, evidence shape, or product exposure requires its own scenario-derived contract and complete dependency closure. |

## 4) Current evidence gaps, in order

The current gap is W8 design intent in PM-25 and PM-26. No V2 implementation or validation exists yet. W8.1 must prove
the emitted compiler/PDB/TypeSpec/literal shapes, exact candidate-keyed constructed-runtime arguments and slots,
nested-generic arity, constraints/accessibility/assignability, scope precedence, primitive/array topology, and the
thread/context/RVA/frame/generic-context branch dispositions before the remaining V2 contracts and product path can
land. W8 then needs complete generated conformance, thirty-two core independent meaningful synthetic dumps plus one for
every admitted evidence branch, corrected
portfolio metrics, and exact closure evidence. The paragraphs below preserve the completed W0-W7 evidence history.

The W0 service-side gates are no longer an evidence gap: exact pushed completion commit
`3ece32a36eccc06a61025b1b35b58c09f6e4ed09` passed documentation consistency, locked restore, a zero-warning Release
build, 60 semantic and differential tests, 40 fast adapter/harness tests, and 3 required Windows dump tests in
[GitHub Actions run 29309374548](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29309374548),
completed 2026-07-14 UTC (2026-07-13 PDT). This run includes the harness start/readiness failure regressions and the
repository-owned local-Markdown-link gate; it supersedes the narrower initial service baseline.

For the historical W1 matrix, local headless verification at `f85545c0c` on 2026-07-14 passed locked restore, a
zero-warning/error 15-project Release build, 64/64 core tests, 63/63 fast integration tests, 3/3 ordinary dump tests,
and 1/1 optimized-context test with no skips or UI. [GitHub Actions run
29353198889](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29353198889) passed all four required jobs at
exact closure commit `e2580a8a8`.

W2 v1 implementation and local headless verification are complete at `ff7cd1965`. The 22-case/20-expression versioned
corpus closes the scenario/replay gate and replays every case within one session and after dump reopen/root rebind;
focused parser, root, descriptor, and plan tests prove the remaining clauses of the narrow product contract. W2 is
complete for its milestone-selected scope at exact closure commit `5bed47100`; [GitHub Actions run
29364905178](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29364905178) passed all four required jobs.

W3 strengthened implementation checkpoint `19c292f9f` realizes a cumulative `+8,842/-1,650` hand-written LOC from the
normative-contract checkpoint (plus 39 generated lock-file lines): `+5,362/-928` production and `+3,480/-722`
tests/fixtures. Its local headless matrix passed locked restore; a 15-project Release build with 0 warnings and 0 errors; 103/103 unit
tests; 67/67 fast integration tests; 5/5 ordinary dump tests; 1/1 optimized modeled-context test; and a separate
focused 2/2 W3 invocation. The focused invocation is an additional view of the W3 facts, not an addition to the
ordinary-dump total. No test was skipped, no UI was displayed, and every test command used the milestone-selected
set at that commit.

The [normative W3 contract](../proposals/architecture/concrete-il-execution-contract-proposal.md) has eleven required
evidence gates. Their implementation-checkpoint traceability is:

1. Structural type, method, and field identity, including cross-module non-aliasing: passing in metadata-identity and
   structural-handle tests.
2. SRM projection for static/instance arguments, `void`/`Int32` returns, initialized locals, and FieldDefs: passing in
   `MetadataIdentityTests` without Reflection-derived interpreter shape.
3. Structured rejection of unsupported signatures, local shapes, field tokens/definitions, EH, and opcodes: passing
   in metadata and machine negative tests.
4. Metadata-derived activation with no caller-supplied counts, locals, or return disposition: passing in activation
   tests over frozen resolved definitions.
5. Typed whole-body admission with no execution of a supported prefix before a rejected suffix: passing with expected
   bounded resolution calls, zero memory calls, and unchanged state, memory, budget, and events.
6. Concrete-domain and persistent-memory laws, including allocated defaults and absent imported fields: passing in the
   domain and memory law suites.
7. Direct and adjusted getters with exactly one real memory-model load and unchanged memory: passing in machine and
   counted-dump getter tests.
8. CoreCLR differential agreement for arithmetic, unchecked overflow, getters, and null receivers: passing over
   compiler-emitted fixtures, with Reflection used only for the live outcome oracle.
9. Same-session and fresh-session canonical replay: passing for fresh metadata/resolver/machine/memory construction and
   for dump close/reopen/module-root-method-field rebind.
10. Generated real-dump E2 execution from counted method metadata/body plus exact field bytes: passing in
    `W3DumpGetterExecutionIntegrationTests` with the disk PE retained only as a late equality oracle.
11. Repository-wide Release build and required milestone-selected test lanes: passing locally with the exact totals
    above and at exact documentation-closure commit `de6cea124488d503d13c61a4c8e67203a16d06f9`; [GitHub Actions run
    29375584237](https://github.com/VladimirReshetnikov/Interpreter/actions/runs/29375584237) passed all four required jobs.

W3 is therefore complete for its defined milestone-selected architecture-validation scope. W4.2 supplies the second
meaningful value domain, and W4.3 demonstrates shared exact/approximate field-handler reuse; neither establishes an abstract
analysis mode. These facts do not reopen the completed W1 or W2 slices.

W4 is closed by the [normative counterfactual-method
contract](../proposals/architecture/counterfactual-method-evaluation-contract-proposal.md) and PM-09 through PM-17.
PM-16 records the canonical product facade/runner; PM-17 records detached rooted dump execution, close/reopen replay,
and exact pushed hosted closure. No W4 evidence gap remains. Allocation is unadmitted by the selected workflow and is
therefore an explicit non-applied bound, not a missing counter or validation gap.

PM-18 and PM-19, and therefore W5, are closed for their stated prototype scope: the W2/W4 capabilities are reachable
from one closed expression-to-result path, and twelve independently dumped designed incidents across two shapes select
fixed-depth member navigation before more prototype behavior is admitted. Exact source baseline `26ba75fb1` passes the
complete local matrix. The owner waived W5's final hosted-only condition on 2026-07-16; the zero-step hosted
rejections remain explicitly absent evidence rather than passing validation.

PM-20 through PM-22, and therefore W6, are closed for their stated prototype scope. The complete Roslyn expression
parser feeds only versioned project-owned tree recognizers; dump-backed declaration certificates, counted reference
evidence, truthful non-root identity and path provenance, the immutable plan, and no-rebinding evaluation implement the
admitted member chain. Eleven generated snapshots prove conformance, and twenty-four independent designed incidents
across four shapes select one context-acquisition category without implementing it. Exact source baseline `440053ad1`
passes the complete local matrix. The separate W6-only hosted disposition remains a governance decision rather than
hosted pass evidence.

PM-23 and PM-24 are complete for W7's stated synthetic scope. W7 proved the physical static slot and exact raw
value/reference path on a dedicated generated fixture, then implemented bounded Roslyn-tree projection, unique
counted-metadata binding, one selected-frame/identity-validated-PDB import-context producer, and typed no-fallback
value/suffix composition. Its materialized sixteen-dump/four-shape portfolio and two fresh-process reports now supply
the validation that the earlier manifest alone could not. Complete Roslyn parsing is already closed by PM-22; W7 extends only versioned tree admission
and project-owned binding and cannot satisfy its context gap with a text recognizer or caller-asserted symbol.

Representative production measurement remains a separately scoped field-readiness follow-up, not a reason to
reinterpret designed incidents as external observations. Caveat: W5–W7 evidence covers only the named generated
and synthetic fixtures and explicitly admitted input shapes; other shapes are not recorded as milestone evidence gaps.

## 5) Maintenance rules

Update this map when:

1. a product requirement or roadmap relation changes,
2. executable implementation evidence lands or is removed,
3. a validation test changes its proof boundary,
4. a design claim is found to have no corresponding evidence gate.

Before declaring a milestone complete, every active row it claims must name passing validation evidence or an explicit, accepted exclusion. Research rows never gate an active milestone merely because their proposals exist.
